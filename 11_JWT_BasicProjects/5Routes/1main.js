//check ueraname ,passward in post(login) request
// if new create new jwt
//sent to fornend

//setup authenthication   so only request with jwt can acess dashward
// jwt.sign(payload, secretKey, options)

// payload → wo data jo hum token ke andar rakhna chahte hai (jaise userId, username).

// secretKey → ek chhupi hui key jo sirf server ke paas hoti hai (stamp banane ke liye).

// options → expiry time, algorithm, etc.